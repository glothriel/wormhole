---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    application: wormhole-client
  name: wormhole-client
spec:
  replicas: 1
  selector:
    matchLabels:
      application: wormhole-client
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        application: wormhole-client
    spec:
      containers:
        - image: ghcr.io/glothriel/wormhole:latest
          name: wormhole
          imagePullPolicy: Always
          command:
            - /usr/bin/wormhole
            - mesh
            - join
            - --name
            - mysterious-peer
            - --expose
            - name=mock-client,address=mock-client:1234
            - --server
            - ws://wormhole-server:8080

